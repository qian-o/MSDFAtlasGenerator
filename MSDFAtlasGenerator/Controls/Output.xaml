<UserControl x:Class="MSDFAtlasGenerator.Controls.Output"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:models="clr-namespace:MSDFAtlasGenerator.Models"
             mc:Ignorable="d"
             d:DesignHeight="450"
             d:DesignWidth="800"
             x:Name="Main">
    <Grid>
        <ItemsControl ItemsSource="{Binding ElementName=Main, Path=OutputData.Logs}">
            <ItemsControl.ItemTemplate>
                <DataTemplate DataType="models:Log">
                    <TextBlock Margin="2"
                               Text="{Binding Message}" />
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>
    </Grid>
</UserControl>
